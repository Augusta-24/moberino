# Changelog — Main-Site Theme Overlay Setup

- Added early no-flash theme boot script to `index.html`.
- Added `css/site-themes.css`.
- Added `js/site-theme.js`.
- Added `THEME_OVERLAY_README.md`.
- Current theme is only `classic` and preserves the existing visual tokens.
- Seasonal `body.theme-*` classes were not changed.
- Arcade files were not intentionally changed.
